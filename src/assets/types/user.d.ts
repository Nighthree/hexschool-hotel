type LoginInputSchema = {
  email: string;
  password: string;
};

type UserAddress = {
  zipcode: number;
  detail: string;
};

type RegisterInputSchema = {
  name: string;
  email: string;
  password: string;
  phone: string;
  birthday: string;
  add;
};
